stack: jussir/loki
description: |
  Lokkaushomma
version: '2.5'
services:
  elasticsearch:
    affinity:
      - label==loki-kone
    image: image-registry.leonidasoy.fi/loki-elasticsearch:v3
    ports: []
    volumes: []
    stateful: true
    extends:
      file: docker-compose.yml
      service: elasticsearch

  fluentd:
    affinity:
      - label==loki-kone
    image: image-registry.leonidasoy.fi/loki-fluentd:v3
    ports: []
    volumes: []
    extends:
      file: docker-compose.yml
      service: fluentd
    links:
      - platform/lb1
    environment:
      - KONTENA_LB_MODE=tcp
    depends_on:
      - elasticsearch

  kibana:
    affinity:
      - label==loki-kone
    image: image-registry.leonidasoy.fi/loki-kibana:v3
    ports: []
    volumes: []
    extends:
      file: docker-compose.yml
      service: kibana
    environment:
      - KONTENA_LB_MODE=http
      - KONTENA_LB_BALANCE=roundrobin
      - KONTENA_LB_INTERNAL_PORT=5601
      - KONTENA_LB_VIRTUAL_HOSTS=loki.leonidasoy.fi
      - "KONTENA_LB_CUSTOM_SETTINGS=redirect scheme https if !{ ssl_fc }"
    links:
      - platform/lb1
    secrets:
      - name: KONTENA_LB_BASIC_AUTH_SECRETS
        secret: BASIC_AUTH_FOR_KIBANA1
        type: env
    depends_on:
      - elasticsearch
